---
id: 2443
repo: "gitlab-org/cli"
title: "feat(token-cache): add token-cache commands for GitLab Agent tokens"
url: "https://gitlab.com/gitlab-org/cli/-/merge_requests/2443"
mergedAt: "2025-11-19T09:29:19Z"
relatedIssues: ["https://gitlab.com/gitlab-org/cli/-/issues/7953"]
summary: "This merge request introduces new `glab cluster agent token-cache` commands, providing users with visibility and control over cached GitLab Agent tokens."
---

## What was done
- Implemented a new `glab cluster agent token-cache` command group.
- Added `glab cluster agent token-cache list` to display cached agent tokens from keyring and filesystem, supporting filtering by agent IDs and cache sources.
- Added `glab cluster agent token-cache clear` to remove cached tokens, with an optional revocation on the GitLab server.
- Both `list` and `clear` commands support `--agent`, `--keyring`, and `--filesystem` flags.
- The `clear` command includes a `--revoke` flag (defaulting to `true`) for server-side token revocation.

## Impact
- Users can now manage and view their cached GitLab Agent tokens directly, eliminating the "black box" behavior of the credential cache.
- Enhances control and debugging capabilities for users utilizing the Kubernetes credential plugin.
- No breaking changes were introduced.

## Technical details
- New commands interact with the system keyring and filesystem for managing cached tokens.
- Integrates with the `glab cluster agent` command structure.

## Related issues
- [#7953](https://gitlab.com/gitlab-org/cli/-/issues/7953)

## Metadata
- Source: gitlab
- Repo: gitlab-org/cli
- URL: https://gitlab.com/gitlab-org/cli/-/merge_requests/2443
- Merged: 2025-11-19T09:29:19Z