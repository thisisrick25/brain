---
prNumber: 350
repo: "brainglobe/brainglobe.github.io"
title: "docs(brainglobe-space): standardize import alias in examples"
url: "https://github.com/brainglobe/brainglobe.github.io/pull/350"
mergedAt: "2025-08-07T10:25:28Z"
summary: "This PR standardizes the import alias for `brainglobe_space` from `bg` to `bgs` across all Python code examples in the documentation."
---

# Contribution Summary

## What was done

- The import alias for the `brainglobe_space` library was updated from `bg` to `bgs` in all relevant Python code examples within the `brainglobe.github.io` documentation.
- The change aimed to standardize the alias used for `brainglobe_space` throughout the entire documentation, ensuring consistency for users.

## Impact

- Improves the consistency and readability of code examples across the `brainglobe.github.io` documentation, making it easier for users to follow and understand.
- Reduces potential confusion that could arise from different aliases being used for the same library in various examples.
- Establishes a standard alias (`bgs`) for `brainglobe_space`, promoting best practices and unified usage.

## Technical details

- The changes primarily involved modifying Python code snippets embedded within documentation files (likely Markdown files) to reflect the new `import brainglobe_space as bgs` convention.
- The PR directly addresses and closes issue #344, which likely called for this standardization.
- The modifications were applied to examples where `brainglobe_space` was previously imported as `bg`.